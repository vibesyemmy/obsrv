---
title: "Cold-machine first run, on each surface"
column: doing
owner: "Henry"
waiting: ""
kind: readiness
criterion: A3
order: 16
---

Never done on a machine that has never run Obsrv. Read the output as a stranger would.

## Claimed by Henry 2026-09-17, on Wren's routing — the plan, before any of it is done

**A fresh CI runner is a machine that has never run Obsrv**, so no desk is needed and nothing is warm.
One throwaway workflow on `probe/a3-cold-first-run`, **three jobs, three runners**, so each surface is
cold on its own and not warmed by another surface's download:

1. **CLI:** `npx -y getobsrv --help`, then the first `snap` of a real page, then the same `snap` again
   (warm), with timings and full stdout/stderr.
2. **MCP:** a minimal MCP client starts `obsrv-mcp` from the published package. It records the tool
   list as a client sees it, then the first `obsrv_presets` and `obsrv_snap` exactly as they come back,
   including whatever the live-first launch does on a machine with no app.
3. **App:** the latest GitHub Release DMG, mounted and launched. It records the first window's
   screenshot and visible text, plus whatever macOS says about an unsigned app.

**Faithfulness:** the published artifacts only (npm `latest`, the latest release), and no
`actions/setup-node` npm cache or `npm ci` of the repo before a capture, because either one would warm
the machine. **The output is read by hand, as a stranger would read it**, and findings are filed as
cards, not as fixes made in the same pass.

## FIRST PASS 2026-09-17 by Henry — three fresh runners, published artifacts, read as a stranger would

**Runs:**
- `35167299615`: CLI, MCP and app, npm `latest` / release 0.60.0.
- `35167885454`: the app again, reading the right window.
- `35168190686`: the MCP session again against **main's packed build**, to tell unreleased fixes from
  current defects.

**Filed as cards:**
- `bug-mcp-cold-first-call-misexplains`: the first MCP call waits 12 s, promises "the next call will
  find it", and the next call gives a different wrong reason. **It reproduces on main.**
- `chore-readme-documents-unreleased`: `obsrv --version` per the README downloads Electron and answers
  "unknown command" on npm 0.60.0. It's fixed on main but unreleased.
- `ux-first-launch-no-calibration`: the first window never mentions the monitor diagonal. A product
  question.

**Read and fine:**
- `npm i -g getobsrv` took 6.6 s.
- `install-skill` said where it installed, and that running sessions need a restart.
- The first `snap`, after Electron's download, took 2.4 s with an accurate one-line summary on stderr,
  and a warm one 1.5 s.
- `audit https://example.com` was 0.8 s.
- The MCP tool list and `obsrv_presets` came back promptly.

**Noted, not filed:** every CLI run on the runner wrote six `Electron Helper … XPC error for connection
com.apple.backupd.sandbox.xpc: Connection invalid` lines to stderr. That's probably the runner (no backup
daemon), but it hasn't been seen on a desk, so it isn't claimed either way.

**What this pass could not see:**
- **A browser-downloaded DMG.** `gh release download` sets no quarantine flag, so the "damaged" dialog
  the README warns about didn't appear. `codesign` reports the app ad-hoc signed, with "code has no
  resources but signature indicates they must be present".
- **What a desk shows when the MCP call launches the app.**
