---
title: "Note inventory: every note seen to fire on a real page"
column: doing
owner: "Henry"
waiting: ""
kind: readiness
criterion: C5
order: 11
---

The 2026-09-13 sweep did this for notes and found gaps; never completed.

## Claimed by Henry 2026-09-17, on Wren's routing — the plan, before any of it is done

**The standing record is `docs/note-inventory.md`** (merged, 856d268). It lists 17 live-surface notes
with 3 observed, and 41 headless/MCP call sites that were never hand-checked. Its line numbers are
from 2026-09-14.

**The ground has moved.** A mechanical count on `main` (`bf81217`) finds 123 call sites that push to
`warnings`/`notes` (cli 65, mcp 37, main 18, preload 3), against 58 then. Many push the same sentence
from a shared builder. So:

1. **Re-inventory by producer, not by call site.** List each distinct sentence (a `src/shared` note
   builder or an inline literal), which surfaces emit it, and where. That's mechanical, and it says so.
2. **Hand-check each producer against the suite.** The question is whether an e2e run against a page
   produces the state, not whether a test mentions the text: the phrase sieve undercounts, as the
   09-14 pass measured. Only what was read by hand gets recorded.
3. **For each unobserved note, pick one of three:** a fixture that produces it, observed on CI (live on
   the runner, whose offscreen target renders; no desk); a finding that the state can't be reached,
   and then the sentence is proposed for removal; or a named reason it stays unobserved.

**Deliverable per step:** `docs/note-inventory.md` updated in place, in board-sized PRs, never as one
large change.

## PROGRESS 2026-09-17 by Henry: what fired (#198), then what can be written and didn't

**Step 2 came first, as a measurement rather than a hand check (#198, merged 74f15c5).** A probe
logged every `notes`/`warnings` string in every reply during one full CI suite (run `35166342003`), and
each sentence was placed at the literal it came from. 57 rows fired. The card never said so.

**Step 1, by producer, at `b43a272`:**
- **Sinks:** a TypeScript-compiler pass (analysis only, not committed) found 173 sinks.
- **Producers:** it followed each value back to 121 sentence producers, with nothing left
  unresolved.
- **Placement:** 109 of the run's 110 distinct sentences place at a producer. The dev-lane stamp
  doesn't, because it's added under a computed key.

**The split:**
- 55 fired;
- 7 are ambiguous (the same text at several places);
- **54 weren't seen to fire**;
- 4 were written or reworded after the run;
- 1 is too short to match.

**Set aside:** ten sentences a first pass counted. Eight are `log.warn` (the log file) and two are
`strictOutput.ts`'s `warn` (stderr), so no reply carries them. Seven of the eight are rows of the
09-14 live table, so that table overstated the notes a reply can carry.

**Next, step 3 in board-sized PRs:** the 54 and the 4, each to a fixture, a removal or a named reason.

## PROGRESS 2026-09-17 by Henry: step 3's first twelve, and what the rest would take

**Twelve sentences now fire on CI**, each pinned by a test asserting the **whole** sentence, and each
shown to fail when that sentence is altered in `src/`:
- **`#256` (seven), suite `35192541543`, control `35192500426`:** custom dimensions in auto mode on
  snap, audit, lint and inspect; `capture: 'pane'` headless; `OBSRV_HEADLESS=1`; `waitMs` live. All
  seven confirmed to have run, not inferred from a green suite.
- **`#258` (five), suite `35194508818`, control `35194537378`:** the composite readout (both routes,
  with an opaque twin that stays silent); text at 1:1; `initial-scale` above 1; the `orientation` word
  inverting; live inspect's headless-only key — which also settles one of the three ambiguous groups,
  since the run's log tags MCP entries with the tool that answered.

**Three needed a fixture nobody had:** text that is not opaque, text exactly the colour of its
background, and a page asking for `initial-scale=2`. The rest needed only a call nobody had made.

**A test of mine was wrong and its control said so**, by failing at the shape assertion rather than
the sentence: it expected `screenShape` on a headless snap, and the key is `Live only`. The test now
pins the absence, and the two places that overstated it — the skill (`#259`) and
`snapOutputShape.rotated`'s own description (`#260`, Wren's find) — are corrected.

**Left: 43 unfired and the 4 written since**, clustered on the doc by what each would take: live-app
races (10), walk limits (13), launch and consent failures (5), truncation caps (7), two sentences only
an older app can produce, and six others. The two remaining ambiguous groups are named there too.

## PROGRESS 2026-09-17 by Henry: the caps and the refused selector, and 39 left

**Four more fire**, on Wren's routing that a cluster a fixture can reach comes before one a race can't:
- **`#263`, suite `35199168099`, control `35199207359`:** three cap sentences on one new fixture,
  `over-caps.html` — 2100 buttons, 3100 paragraphs and 600 upscaled images, past `AUDIT_MAX_TARGETS`,
  the 3000 text cap on both tools, and `LINT_MAX_IMAGES`. Every fixture until now fitted inside every
  cap. The control's received values are the numbers it was sized for, and they corrected an
  assumption: `edges` is 0, since a button's own border is not counted.
- **`#264`, suite `35199881465`, control `35200051529`:** the fourth outcome of `scrollSelector` — a
  string the browser refuses. Its **first control was invalid and said so by how it failed**: run with
  `-g` on one test, it went red in 79 ms at that test's *first* assertion, because the test depends on
  state its predecessors leave. A filtered control removes the predecessors, not the product.

**The probe's own baseline is now written down on the doc**, as an instruction rather than a result:
check that every logged sentence places at a producer before trusting any count, because a pass that
misses a class of sink reports a smaller, perfectly plausible number. This one was run because 121
producers could not be reconciled with the earlier sieve's 84 call sites — an unaccountable delta, not
an instinct — and it caught `log.warn` being counted as a reply.

**The launch cluster's safety is settled before anyone writes it** (Wren's read): all five sentences
sit behind the guard that exists so the harness never launches a real Obsrv against the developer's
profile. The doc carries the measured shape — the harness already launches the built app into a temp
profile with logs redirected, and only `launchApp`'s electron target can carry that — so a gate goes
on that path alone, and a local run needs Opeyemi's separate yes.

**Routed out:** the walk limits (13) to Rook, with a measurement as the first deliverable — which of
the 13 the nine existing walk fixtures already produce, read from the JSON — since only what is left
after that needs a new fixture.

**39 unfired, and the 4 written since.**

## PLAN 2026-09-17 by Henry: the last cluster (the six others), measured before any test

Routed by Wren as the last group with nobody on it. **Measured first**, as the other clusters proved
worth: five of the six are forceable, one is a named reason, and the measurement turned up a defect.

| producer | the state it needs | forceable? |
| --- | --- | --- |
| `cli/capture.ts:347` | `unsettledReason: 'uncovered'` — part of the frame never painted inside the budget | **yes**, with a fixture that paints one region late; the mask is what the sentence counts |
| `cli/stuckProbe.ts:98` | the page replaces its document **once** during the stuck-chrome probe, which only runs under `--full-page` with more than one band | **yes** — a tall page that replaces itself on the probe's first scroll |
| `cli/stuckProbe.ts:76` | the same probe's `fail()`: a mark or settle that throws, an unreadable answer, or a document replaced **twice** | **yes**, by the same fixture replacing on every scroll |
| `mcp/server.ts:871` | a live navigate that the app answers with `loading: true` | **yes** — `never-loads.html` through `obsrv_drive`, since the harness shortens the app's budget |
| `mcp/server.ts:1070` | a live snap of a page already showing, still loading past `LIVE_SETTLE_MS` | **yes** — drive to `never-loads.html`, then snap the same URL so `navigated` is false |
| `mcp/server.ts:1092` | an app older than the capture's settle verdict | **no** — version skew, the third of that group with `mcp/walk.ts:41` and `walkCoverage.ts:194` |

### The inventory's tests found a product defect by refusing to write a false one

**This cluster's measurement turned up a sentence that states a figure it does not hold, and the way
it turned up is the argument for measuring first:** the test could not be written without enforcing a
falsehood. Every c5 test asserts the whole sentence; asserting this one would have pinned "(30 s)"
over a budget of 8 s. So the audit's own standard refused the test, and the refusal was the finding.

Fixed in `#279` — the server keeps the outcome and drops the number. **Bookkeeping (Wren):** it is a
user-visible warning wording change, which `docs/compatibility.md` sanctions in a minor, so it needs
no register entry today; when an unreleased section opens for the version after 0.61.0, it belongs in
it as a sanctioned wording change.

### A defect the measurement found, and it blocks one of its own tests

**`NAVIGATE_CUT_NOTE` hard-codes a number it does not read:**

    "the page was still loading when the app's navigate budget (30 s) ran out; …"   (server.ts:871)

The app's budget is `NAVIGATE_WAIT_MS` (`ipc.ts:365`), which **defaults to 30 s and is overridden by
`OBSRV_NAVIGATE_WAIT_MS`** — the harness sets it to 8 s (`tests/e2e/launch.ts:42`). So in the field
the sentence is right, and anywhere the variable is set it states a figure that is not the budget.

**It blocks its own test**, which is how it was found: firing this note in the suite and asserting the
whole sentence would pin **"(30 s)"** while the budget that actually ran out was 8 s — a test
enforcing a falsehood. So the sentence is fixed before it is pinned: either the app reports the budget
it used, or the note drops the figure. **Engineering, mine, and it goes first.**

## DECISION 2026-09-17 by Henry: the inventory buys no fences

Asked by Kenya before building (room #427): a second `OBSRV_TEST`-fenced hook in `src/`, to force
`frameCheck:21` — *"the renderer did not say which frame it drew"* — by never sending `drawNow`.
Carefully written, fenced twice, inert when unset. **Declined, and the rule is general:**

> **A sentence reachable only by changing the product is recorded as reachable, citing the control
> run that produced it, and parked. A test-only fence in `src/` has to be bought by a defect, not by
> an audit.**

**Why.** The observation already exists without the fence: `#139`'s control removed the `drawNow`
send and the sentence appeared in a real reply. That is the standard every control on this board is
held to, so the claim — *"this sentence is reachable when a draw is never acknowledged"* — is already
supported, and citing the run is the whole of what the fence would add to the record.

**What the fence would really buy is a standing regression test**, and its cost is that it sits in
the path every capture takes. A permanent risk to every capture, in exchange for pinning one
diagnostic sentence with no defect behind it, is the wrong trade.

**The precedent it would have leaned on cuts the other way.** `OBSRV_TEST_THROTTLE_REFUSAL` exists
because a real bug shipped a refusal to stderr only and needed a standing guard: **the defect bought
the fence.** c5 is an audit of what the product says in the field, and an audit that edits its
subject to make it speak has stopped measuring it.

**If `frameCheck:21` ever bites** — a stale capture nobody was warned about — that defect buys the
fence, and the sentence gets its standing test then.

## PLAN 2026-09-17 by Henry: the launch cluster (5), and why the guard stays untouched

Claimed on Wren's routing. **Written down before any code, because the first design was wrong and the
second only exists because the product was measured.**

**The five sentences** are in `ensureLive` (`src/mcp/control.ts:310-360`): the app could not be
launched; the launch exited immediately into a profile already in use; consent went unanswered; the
app was launched but did not answer; agent control is off (`lib.ts:581`).

**The first design, which is not being built.** All five sit behind `OBSRV_TEST`, whose guard says
why — *"the e2e harness must never launch a real Obsrv"* (`mcp/lib.ts:617`), with `launchApp`
refusing again at `mcp/launch.ts:91`. I proposed gating an exception past it. **Wren's rule — measure
whether the product can already do it before designing around a guard — applied to my own proposal,
and the answer is that it can.**

**The design: the dev lane, which already ships the isolated path.** `scripts/devLane.js:123`
`appLaunch(root, env)` returns the built tree's entry (`<root>/out/main/index.js`), an isolated
profile (`--user-data-dir=<devHome>/profile`, where `devHome` is **`OBSRV_DEV_HOME`** or
`~/.obsrv-dev`, `:22-29`), and a window titled "dev lane" so it is never taken for the installed app.
`mcp-live.spec` is the precedent for the other half: it runs the MCP server **without** `OBSRV_TEST`.
So the spec sets `OBSRV_DEV=1` and `OBSRV_DEV_HOME=<mkdtemp>`, every launch goes to the built tree on
a throwaway profile, and **`cannotLaunchReason` and `launchApp`'s throw stay exactly as they are for
every other caller.** No test-only code in `src/`.

**The hazard that design carries, and how the spec closes it** (Wren): `profileDir` falls back to
`~/.obsrv-dev` when `OBSRV_DEV_HOME` is unset, so a spec that forgets it — or a refactor that drops
it from the env — would silently drive **Opeyemi's own dev lane and window**, and nothing in the run
would say so. The spec reads `OBSRV_DEV_HOME` at setup, asserts it is set and under `os.tmpdir()`,
and fails with *"this spec must not run against the real dev lane"* otherwise. Loud in exactly the
case where silence costs most.

**Constraints on the work:**
- **CI only**, and **a local run needs Opeyemi's separate yes**, as the desk-taking specs do: it opens
  a real window.
- A stray window on a runner is identifiable in a screenshot by its "dev lane" title.
- **A unit test through the injected deps is not evidence** — it constructs all five and observes none.

**Reachability, as far as reading goes:**
- *the launch exited immediately, profile already in use* — clean: the harness holds the lane profile
  and the server's instance loses the lock;
- *nobody answered the consent prompt* — a lane app with control off, and the 12 s `LAUNCH_TIMEOUT_MS`;
- *the app could not be launched* — to check: a lane with no built entry fails in `resolveDevTarget`,
  which may produce a different sentence;
- *launched but did not answer* — **parked with a reason** rather than built, since reaching it
  wants a fake app that comes up and stays silent, and a fake asserts our belief about a state instead
  of observing it.

### The launch cluster's outcome, 2026-09-17: two fire, two are named reasons

**Merged as `#274` (9676dca):** *profile already in use* and *consent unanswered*, both produced through
the dev lane on a temp profile, with the guard untouched. Suite `35205619883` (4.8 s and 15.8 s),
control `35205639766` red at both sentence assertions — and the control proved the mechanism as well
as the assertions, since the altered sentences could only appear if both states had really happened.

**`control.ts:318` — *the app could not be launched* — is parked, and the control is what parked it.**
`#277` tried the obvious lever, `OBSRV_ELECTRON_PKG_DIR` pointed at a directory that is not an electron
package: `resolveElectron()` fails fast with no download (measured), so `defaultDeps.launch` throws.
But run `35207583374` went red at **`expect(r.isError).toBeFalsy()`**, not at the sentence — because
**the same resolver serves the headless render**, so the fallback the note accompanies
(*"rendered headlessly"*) cannot happen and the call errors instead of answering with the note.

The remaining levers are worse than the sentence is worth:
- **outside dev mode**, `resolveLaunchTarget` searches `/Applications` and `~/Applications` and then
  falls back to the package's own build — on CI it would *launch* the checkout's app rather than
  error, and on a desk with Obsrv installed it would launch the installed one, which is the thing this
  cluster's design exists to prevent;
- **inside dev mode**, the error needs `out/main/index.js` missing from the server's own package root,
  which means a fabricated root (a real `out/mcp`, symlinks for the rest, since `__dirname` resolves
  through symlinks to the real tree) built for this arm alone.

**So: reachable in the field** — a user with no Obsrv installed whose package cannot run its own —
**and not reachable here without machinery whose only purpose is the arm.** Named reason, with
`control.ts:357` beside it.

## PROGRESS 2026-09-17 by Rook: walk limits, batch 1 — four of the thirteen, and a walk fact nobody had

**Four walk-limit sentences now fire on CI** (`tests/e2e/cli-walk-limits.spec.ts`, each surface a
whole-sentence assertion): `cli/walk.ts:165` and `mcp/walk.ts:197` ("the page stopped moving before
the end of the walk"), `walkCoverage.ts:245` (`walkDialogNote` at zero screenfuls) and
`walkCoverage.ts:168` (`walkNothingNote`'s shadow-root tail, "the page has 1 open shadow root").
Green run `35202725398`, all six tests listed as run. Control `35203138455`: all six red **at the
sentence assertion**, every `Received` carrying the `CONTROL `-prefixed sentence — not at a count.

**Measured first, then built** (run `35200677199`, every existing walk fixture headless and live):
the existing fixtures fire these sentences' already-counted siblings and none of these, so each got a
fixture for its one branch — `locks-mid-walk.html`, `dialog-sideways.html`, `shell-with-one-root.html`.

**A walk fact the first run found, now on the record:** `overflow: hidden` does not block a scripted
`scrollTo`, and both surfaces scroll the root with `window.scrollTo({behavior: 'instant'})` and read
back in the same task. So a page that locks its scroll from a scroll handler stops the walk one step
LATER than the lock — 3 screenfuls on `locks-mid-walk.html`, not the 2 a simulation predicted. The
simulation awaited a frame between scroll and readback; the walk has no such frame. The count is tied
to that mechanism in the spec and the fixture, and the sentence is asserted before the count so a
count mismatch cannot hide whether the sentence fired (run `35201784437` did exactly that hiding).

**A second thing the control caught:** the live describe was `serial` by reflex, and the first
control run (`35202881688`) went 4 red, 2 did not run — one live red skipped the other two live
tests, so their verdicts were unread. Serial is gone, with the reason written in the file. It
generalises (Henry, #421): a control that runs a serial file stops at the first red and leaves the
rest with no control at all — silently, since "did not run" looks like "not applicable" in a summary
that only counts failures. Read a control's "did not run" line as loudly as its "failed" line.

**`walkCoverage.ts:194` classified** (room #420, decided #421): reachable only under version skew
— an app older than the `blocked` field (pre-0.58.0, `82884e1`) driven by a newer MCP; unreachable
headless by construction. **Not removable**; a named reason, not an observation; joins
`mcp/server.ts:1092` and `mcp/walk.ts:41` on `chore-minimum-app-version`, the card those three now
share, where one decision lands when a minimum app version is set. A unit test pins the whole sentence as a construction
guard, named for the skew (`walkCoverage.test.ts`), and stays out of the observed column. A
correction to #420: a phrase-level test of the `undefined` shape already existed
(`claims no measurement it was not given`); under the same `CONTROL` alteration it stays green while
the whole-sentence guard goes red, which is the difference the guard adds.

**Left in the cluster: 8.** Batch 2 is the budget pair (`cli/walk:125`, `mcp/walk:143`) and the
cut-short pair (`cli/walk:179`, `mcp/walk:211`); the budget lever gets measured before its fixture is
built. `mcp/walk:184` parked with a reason to be named.

## PROGRESS 2026-09-17 by Henry: six batches folded into the inventory, and the named reasons become a list

**Folded:** `#270` (walk limits batch 1, Rook), `#273` (Kenya's three live-app states), `#274` (the
launch pair), `#282` (the two still-loading sentences), `#283` (the stuck probe's two) and `#292`
(card 9's three). The fired table goes from 16 rows to **32**, each with the run that carried it and
the control that reds it. The unfired list drops from **39 to 14**.

**Nine producers get a table of their own, for a reason this file had been carrying in prose:** a
named reason is not an observation, and a row that reads like unfinished work is a lie about nine
sentences that have been decided. Three are live-app states measured not to occur (12 launches, 3
reloads); one waits for a defect to buy its fence (`frameCheck:21`); two are launch states whose
levers were tried and rejected on evidence; three are version skew and share
`chore-minimum-app-version`.

**The counts reconcile to the pass's 121 producers:** 89 fired and placed, 4 ambiguous (2 groups), 14
unfired, 9 named reasons, 4 written or reworded after the run, 1 too short to match.

**What is left, by what it would take:** walk limits (8), truncation and caps (3), and three others —
an uncovered frame, image findings below the walk's height, and agent control turned off. The
clusters section is rewritten against those 14 rather than the original 43.

### `#293` is the first change to take sentences off the list rather than fire them

Three retire, because the gap each described is gone: the share note, the empty-page note's
web-components branch, and — for a walk that entered the roots — the walk note's shadow-root tail.
The first two are **deleted producers**, which is a different outcome from "unfired" and the
inventory now says so in its own paragraph rather than leaving two rows to read as work nobody did.

**The third does not disappear; it changes category**, and so does the older walk's ruled-out
wording. Both are reachable from an app between 0.58.0 and `#293`, driven by a newer MCP, and the
walk note keeps their words exactly for that app. **They join the version-skew group — and
`chore-minimum-app-version` says a fourth sentence joining it is what would make its decision
urgent. Two joined at once.**

`#293` also wrote three sentences nobody has seen fire, which go in the reworded table rather than
the unfired list: the walk note's new opening, its ruled-out wording for a walk that entered the
roots, and the capture's reworded "scrolls nothing the capture can reach".

## PROGRESS 2026-09-17 by Henry: walk limits batch 2 — the budget pair, and the two a page that stops answering produces

**Four sentences, two fixtures, both surfaces**, in `tests/e2e/cli-walk-limits.spec.ts` beside Rook's
batch 1:

| producer | the sentence | the fixture |
| --- | --- | --- |
| `cli/walk.ts:125` | the walk stopped after N screenfuls at its 15 s budget without reaching the end of the page | `taller-than-the-walk-budget.html`, 150 screenfuls |
| `mcp/walk.ts:143` | the same, live | the same page |
| `cli/walk.ts:179` | the walk was cut short after N screenfuls (…); measured after a partial walk | `blocks-on-scroll.html`, which answers the first scroll then holds its main thread |
| `mcp/walk.ts:184` | the page did not confirm a scroll during the walk; the walk stopped there | the same page, held 5 s |

**The two surfaces meet the same page differently, and that is the point.** Headless, the next step
is an `executeJavaScript` that cannot run until the thread lets go, so it times out on the walk's own
budget and the step throws — carrying `walkTimeoutNote`'s sentence inside the cut-short one. Live,
the scroll goes through the control server, which answers `scrolled: null` after its 1 s confirm
window rather than throwing. **`mcp/walk.ts:184` is the sentence batch 1 parked**; it needed the
other half of this fixture.

**The hold had to be measured, not chosen.** At 25 s the live audit was refused outright — *"obsrv
control audit timed out after 20000 ms"* — because the audit measures the page **after** the walk and
needs the same main thread, so the walk's sentence never reached a reply (probe `35229526006`). The
fixture takes its hold from the query: 25 s headless, 5 s live, and it says why.

**Green:** probe `35230375653`, ten of ten. **Control:** `35231440742`, the four producers altered —
red at `:156`, `:170`, `:266` and `:285`, each at its own sentence assertion, with batch 1's six
still green beside them.

**The cluster:** 13 sentences at the start, 4 in batch 1, 4 here, **5 left** — the return-to-top pair,
the cut-short pair's live twin (`mcp/walk.ts:211`), and `walkCoverage.ts:174`.

**One of the five now has a lever, and a reason not to use it yet.** `blocks-on-scroll.html` makes
`cli/walk.ts:98` — *"the walk could not return to the top afterwards (…); measured where it
stopped."* — fire on the headless surface, because a cut-short walk calls `backToTop` next. **But the
message it carries is wrong**, which Kenya found reading `#308` and Wren verified in code: `step`
waits `deadline.remaining()` and throws `walkTimeoutNote(budgetMs)`, so after a cut-short the page is
given about 0 ms and the sentence blames its main thread for 15 s of silence. Headless only — the
live path passes no budget of its own, so it has no number to misreport.

**So this batch does not assert it.** Pinning a sentence we have just measured to be wrong is the
mistake `bug-live-raster-uncovered-said-as-painting` was filed for, one week earlier in the same
file. The defect is Kenya's to card, since she found it; when it is fixed, this fixture is already
here and the assertion is one arm.

**And the frequency is measured — by two runs of the same print that disagree, which is the finding.**
This branch printed twice, once per suite, and the headless budget-ended walk answered differently
each time.

`35234853920`, **one** sentence:

```
budget-ended walk said: ["the walk stopped after 98 screenfuls at its 15 s budget without reaching the end of the page; the measurement covers the whole page regardless."]
```

`35239464603`, the rebase's suite, **two** — and the second is the wrong note:

```
budget-ended walk said: ["the walk stopped after 97 screenfuls …","the walk could not return to the top afterwards (the page did not answer a scroll within 15 s (its main thread was busy or blocked)); measured where it stopped."]
```

The live surface printed the same two sentences both times, and the note is in neither:

```
budget-ended live walk said: ["the walk stopped after 43 screenfuls …","`groupsOnly` left the per-finding list out on request: …"]
```

**So the note fires on a budget-ended walk, 1 of 2 headless and 0 of 2 live, and it is a race.**
`backToTop` runs with about 0 ms left and races `withinBudget(executeJavaScript, 0)` against a
`setTimeout(0)`; Wren read that in code and Kenya's fake saw it from the other side (no sentence at
0–1 ms of reply latency, the sentence from 2 ms). The page usually wins on a free main thread. Twice
is not a rate, and this is exactly the shape a single print would have got wrong: the first run alone
said the note does not fire here, and I wrote that down before the second run said otherwise.

**And on this path the sentence is wrong in a second way.** In the cut-short path the page's thread
really is blocked, so only the *number* lies. Here the page answered every one of 97 steps and is
then told it did not answer a scroll within 15 s — **the blame is wrong too, not just the duration.**
That widens `bug-walk-return-note-names-budget` rather than narrowing it, and it is what a fix has to
cover: both the time actually allowed and who is being accused of spending it.

**These four are fired on CI and not yet folded into `docs/note-inventory.md`.** The fold above
counts the inventory as it stood before this run, so its numbers still read 14 unfired and 8 in the
walk cluster. The next fold moves these four: **14 → 10 unfired, 8 → 4 in the cluster.** Saying so
here rather than editing the fold's paragraph keeps each section true about the run it reports.

