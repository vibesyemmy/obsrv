---
title: "Note inventory: every note seen to fire on a real page"
column: doing
owner: "Henry"
waiting: ""
kind: readiness
criterion: C5
order: 11
---

The 2026-09-13 sweep did this for notes and found gaps; never completed.

## Claimed by Henry 2026-09-17, on Wren's routing — the plan, before any of it is done

**The standing record is `docs/note-inventory.md`** (merged, 856d268). It lists 17 live-surface notes
with 3 observed, and 41 headless/MCP call sites that were never hand-checked. Its line numbers are
from 2026-09-14.

**The ground has moved.** A mechanical count on `main` (`bf81217`) finds 123 call sites that push to
`warnings`/`notes` (cli 65, mcp 37, main 18, preload 3), against 58 then. Many push the same sentence
from a shared builder. So:

1. **Re-inventory by producer, not by call site.** List each distinct sentence (a `src/shared` note
   builder or an inline literal), which surfaces emit it, and where. That's mechanical, and it says so.
2. **Hand-check each producer against the suite.** The question is whether an e2e run against a page
   produces the state, not whether a test mentions the text: the phrase sieve undercounts, as the
   09-14 pass measured. Only what was read by hand gets recorded.
3. **For each unobserved note, pick one of three:** a fixture that produces it, observed on CI (live on
   the runner, whose offscreen target renders; no desk); a finding that the state can't be reached,
   and then the sentence is proposed for removal; or a named reason it stays unobserved.

**Deliverable per step:** `docs/note-inventory.md` updated in place, in board-sized PRs, never as one
large change.

## PROGRESS 2026-09-17 by Henry: what fired (#198), then what can be written and didn't

**Step 2 came first, as a measurement rather than a hand check (#198, merged 74f15c5).** A probe
logged every `notes`/`warnings` string in every reply during one full CI suite (run `35166342003`), and
each sentence was placed at the literal it came from. 57 rows fired. The card never said so.

**Step 1, by producer, at `b43a272`:**
- **Sinks:** a TypeScript-compiler pass (analysis only, not committed) found 173 sinks.
- **Producers:** it followed each value back to 121 sentence producers, with nothing left
  unresolved.
- **Placement:** 109 of the run's 110 distinct sentences place at a producer. The dev-lane stamp
  doesn't, because it's added under a computed key.

**The split:**
- 55 fired;
- 7 are ambiguous (the same text at several places);
- **54 weren't seen to fire**;
- 4 were written or reworded after the run;
- 1 is too short to match.

**Set aside:** ten sentences a first pass counted. Eight are `log.warn` (the log file) and two are
`strictOutput.ts`'s `warn` (stderr), so no reply carries them. Seven of the eight are rows of the
09-14 live table, so that table overstated the notes a reply can carry.

**Next, step 3 in board-sized PRs:** the 54 and the 4, each to a fixture, a removal or a named reason.

## PROGRESS 2026-09-17 by Henry: step 3's first twelve, and what the rest would take

**Twelve sentences now fire on CI**, each pinned by a test asserting the **whole** sentence, and each
shown to fail when that sentence is altered in `src/`:
- **`#256` (seven), suite `35192541543`, control `35192500426`:** custom dimensions in auto mode on
  snap, audit, lint and inspect; `capture: 'pane'` headless; `OBSRV_HEADLESS=1`; `waitMs` live. All
  seven confirmed to have run, not inferred from a green suite.
- **`#258` (five), suite `35194508818`, control `35194537378`:** the composite readout (both routes,
  with an opaque twin that stays silent); text at 1:1; `initial-scale` above 1; the `orientation` word
  inverting; live inspect's headless-only key — which also settles one of the three ambiguous groups,
  since the run's log tags MCP entries with the tool that answered.

**Three needed a fixture nobody had:** text that is not opaque, text exactly the colour of its
background, and a page asking for `initial-scale=2`. The rest needed only a call nobody had made.

**A test of mine was wrong and its control said so**, by failing at the shape assertion rather than
the sentence: it expected `screenShape` on a headless snap, and the key is `Live only`. The test now
pins the absence, and the two places that overstated it — the skill (`#259`) and
`snapOutputShape.rotated`'s own description (`#260`, Wren's find) — are corrected.

**Left: 43 unfired and the 4 written since**, clustered on the doc by what each would take: live-app
races (10), walk limits (13), launch and consent failures (5), truncation caps (7), two sentences only
an older app can produce, and six others. The two remaining ambiguous groups are named there too.
